# TOXIC-TOOLS

# Tool Installer

apt update && apt upgrade

apt install git

apt install curl

apt install wget

git clone https://github.com/muhammadfathul/TOXIC-TOOLS

cd TOXIC-TOOLS

chmod 777 toxic.sh

./toxic.sh
